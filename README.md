# Crash-afaePC-to-BOM
Make your PC to Crash!

1.What is this:
	As the name suggests (Crash afaePC to BOM)
	It can crash your PC completely.
2.Effects:
	1.Rapid memory surge, and plus huge pagefile.sys expansion (if pagefile.sys is enabled)
	2.Extreme lag when moving or dragging windows
	2.Systems below Windows 8 will fall back to Basic mode, and the Desktop Window Manager service will be disabled.
3.Built with:
	1.Simply allocate and fill massive memory blocks
	2.Use Windows API to create threads equal to CPU cores for heavy computations.
Tips:
	1.The author takes no responsibility for any damage caused by using this tool.
	2.Only works on Windows (uses Windows API).
	3.DO NOT PUT THIS FILE ON BRO'PC AND ADD THE PATH TO AUTO-RUN.
(machine translation)



1.What is this：
	这个玩意跟名字一样（Crash afaePC to BOM）
	可以让你的电脑卡死

2.效果：
	1.内存暴增，且page.sys暴增（如果打开了page.sys）
	2.移动、拖动窗口非常卡
	3.如果系统在Microsoft Windows 8以下会导致退回到Basic，而且Desktop Window Manger服务会被关闭

3.制作用到的：
	1.只是申请一堆内存然后填满
	2.只是用Windows API创建与CPU核数一样的线程进行复杂运算罢了

Tips:	1.使用这个东西时，作者概不负责
	2.只能在Windows下使用（因为使用了Windows API）
	3.千万不要把这个文件放到bro的电脑上，然后把这个路径添加到auto-run里面
